#include <math.h>
#include "my_math.h"	/* 
			 * Include na sushtiq .h file e nenujno,
			 * no prepuruchitelno.
			 */

double cube(double x)
{
	return pow(x, 3);
}

double square(double x)
{
	return pow(x, 2);
}
